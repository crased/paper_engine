# Paper Engine v1.0 - Python Dependencies
# Complete end-to-end pipeline for AI-powered game bot development

# ============================================================
# CORE DEPENDENCIES (Required)
# ============================================================

# Keyboard and mouse control
pynput>=1.7.6

# Image processing
Pillow>=10.0.0

# Screen capture
mss>=9.0.1

# Environment variable management
python-dotenv>=1.0.0

# YAML parsing for config
PyYAML>=6.0

# Annotation platform
label-studio>=1.9.0

# ============================================================
# TRAINING DEPENDENCIES (Optional - for YOLO model training)
# ============================================================

# YOLO object detection
ultralytics>=8.0.0

# PyTorch deep learning framework
torch>=2.0.0
torchvision>=0.15.0

# ============================================================
# AI/LLM DEPENDENCIES (Optional - for bot generation)
# Choose one or more providers
# ============================================================

# Google Gemini (Default - FREE TIER AVAILABLE)
# Get free API key: https://aistudio.google.com/apikey
google-generativeai>=0.3.0

# Anthropic Claude (Paid)
# Get API key: https://console.anthropic.com/settings/keys
anthropic>=0.18.0

# OpenAI GPT (Paid)
# Get API key: https://platform.openai.com/api-keys
openai>=1.12.0

# ============================================================
# ADDITIONAL DEPENDENCIES
# ============================================================

# JSON processing (usually built-in, but explicit for clarity)
# Already included in Python standard library

# NumPy (required by ultralytics and torch)
numpy>=1.24.0

# OpenCV for image operations (required by ultralytics)
opencv-python>=4.8.0

# ============================================================
# INSTALLATION INSTRUCTIONS
# ============================================================
#
# Full installation (all features):
#   pip install -r requirements.txt
#
# Core only (no training, no AI):
#   pip install pynput Pillow mss python-dotenv PyYAML label-studio
#
# Core + Training (no AI):
#   pip install pynput Pillow mss python-dotenv PyYAML label-studio ultralytics torch torchvision
#
# Core + Google Gemini AI (free tier):
#   pip install pynput Pillow mss python-dotenv PyYAML label-studio google-generativeai
#
# Note: main.py will auto-detect and offer to install missing packages
#
# ============================================================
# SYSTEM REQUIREMENTS
# ============================================================
#
# Linux:
#   - wine: sudo apt install wine (for Windows game execution)
#   - flameshot: sudo apt install flameshot (for screenshots)
#
# Windows:
#   - No additional system tools needed
#
# ============================================================
