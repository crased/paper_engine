import cv2 as cv
import numpy as np
import screenshots
i
#loop through imgs cv and alr annotated data to match them and then figure out rest of the botting situation
def img_selections(screenshots,match):
 with img in screenshots:
   if img.lower().endswith((".png", ".jpg", ".jpeg")):
    #img = cv.imread("img.jpg")
    pass
def comparison_imgs(dataset,match):
 with img in dataset:
    if img.lower.endswith(("placeholder")):
       comp = cv.imread(img.DataType)#datatype is a place holder
       pass
def check_match(screenshots,dataset):
 for .      