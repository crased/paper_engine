import cv2 as cv
import numpy as np
import screenshots

#loop through imgs cv and alr annotated data to match them and then figure out rest of the botting situation
def img_selections(screenshots):
 for img in screenshots:
    if img == #"annotated.img"this will be subject to change:
    #img = cv.imread("img.jpg")
   
def comparison_imgs(dataset)